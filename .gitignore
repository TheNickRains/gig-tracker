# Secrets — never commit
.env
.env.*

# Runtime config holds the (public) anon key; keep it out of git so deploys
# stay the source of truth and the service_role key never sneaks in here.
# In production the server generates /config.js from Railway env vars instead.
app/config.js

# Node
node_modules/
npm-debug.log*

# macOS
.DS_Store

# Scratch / local-only — never commit
notepad*
*.local
